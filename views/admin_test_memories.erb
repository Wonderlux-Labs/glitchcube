<!DOCTYPE html>
<html>
<head>
  <title>Memories</title>
  <style>
    body {
      font-family: monospace;
      max-width: 900px;
      margin: 40px auto;
      padding: 20px;
      background: #f5f5f5;
    }
    h1 { color: #333; }
    .nav {
      margin-bottom: 20px;
      padding: 10px;
      background: white;
      border: 1px solid #ddd;
    }
    .nav a {
      margin-right: 15px;
      color: #2196F3;
      text-decoration: none;
    }
    .memory {
      background: white;
      padding: 15px;
      margin: 15px 0;
      border: 1px solid #ddd;
      border-radius: 4px;
    }
    .memory-header {
      display: flex;
      justify-content: space-between;
      margin-bottom: 10px;
      padding-bottom: 10px;
      border-bottom: 1px solid #eee;
    }
    .memory-content {
      line-height: 1.5;
      color: #333;
    }
    .metadata {
      color: #666;
      font-size: 0.9em;
      margin-top: 10px;
    }
    .intensity-bar {
      display: inline-block;
      width: 100px;
      height: 10px;
      background: #e0e0e0;
      border-radius: 5px;
      position: relative;
      margin-left: 5px;
    }
    .intensity-fill {
      height: 100%;
      background: #4CAF50;
      border-radius: 5px;
    }
  </style>
</head>
<body>

<div class="nav">
  <a href="/admin/test">← Back to Test Interface</a>
  <a href="/admin/test/sessions">Sessions</a>
  <a href="/admin/test/tools">Tools</a>
</div>

<h1>Recent Memories</h1>

<% @memories.each do |memory| %>
  <div class="memory">
    <div class="memory-header">
      <strong><%= memory[:category] || 'general' %></strong>
      <span><%= memory[:created_at] %></span>
    </div>
    
    <div class="memory-content">
      <%= memory[:content] %>
    </div>
    
    <div class="metadata">
      <% if memory[:location] %>
        📍 <%= memory[:location] %> |
      <% end %>
      
      Intensity: <%= memory[:emotional_intensity] %>%
      <span class="intensity-bar">
        <span class="intensity-fill" style="width: <%= memory[:emotional_intensity] %>%"></span>
      </span>
      |
      
      Recalled: <%= memory[:recall_count] %> times
    </div>
  </div>
<% end %>

<% if @memories.empty? %>
  <p>No memories found.</p>
<% end %>

</body>
</html>